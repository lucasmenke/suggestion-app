{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "DatabaseName": "suggestionapp",
  "ConnectionStrings": {
    "MongoDB": "<in the secrets.json>"
  },
  "AzureAdB2C": {
    "Instance": "<in the secrets.json>",
    "ClientId": "<in the secrets.json>",
    "CallbackPath": "<in the secrets.json>",
    "Domain": "<in the secrets.json>",
    "SignUpSignInPolicyId": "<in the secrets.json>",
    "ResetPasswordPolicyId": "<in the secrets.json>",
    "EditProfilePolicyId": "<in the secrets.json>"
  }
}
